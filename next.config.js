/** @type {import('next').NextConfig} */
const nextConfig = {
  // App Router (app directory) is enabled by default in Next.js 13+ when the `app` directory exists.
  // Remove experimental options that are no longer recognized in newer Next.js versions.
};

module.exports = nextConfig;