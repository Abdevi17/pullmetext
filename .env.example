# Example environment variables for PullMeText. Copy this file to `.env.local` and fill in your values.

# Required: OpenAI API key for calling the chat completion API.
OPENAI_API_KEY=your_openai_api_key_here

# Optional: PostgreSQL connection string for Prisma if you choose to enable user accounts and storage.
DATABASE_URL=postgresql://user:password@localhost:5432/pullmetext

# Optional: Stripe API keys if you decide to enable paid plans.
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key

# Optional: NextAuth configuration if you enable authentication (not used in the current skeleton).
NEXTAUTH_SECRET=some_long_random_string
NEXTAUTH_URL=http://localhost:3000

# Optional: Email server configuration for passwordless login (if implementing email sign in).
EMAIL_SERVER_HOST=smtp.example.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your_email@example.com
EMAIL_SERVER_PASSWORD=your_email_password
EMAIL_FROM="PullMeText <noreply@pullmetext.com>"